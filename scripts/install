#!/bin/bash

source _common.sh
source /usr/share/yunohost/helpers

#=================================================
# DOWNLOAD, CHECK AND UNPACK SOURCE
#=================================================
ynh_script_progression "Setting up source files..."

ynh_setup_source --dest_dir="$install_dir"

touch $install_dir/database.db

chmod -R o-rwx "$install_dir"
chown -R $app:$app "$install_dir"
chmod +x "$install_dir/filebrowser"

#=================================================
# SYSTEM CONFIGURATION
#=================================================
ynh_script_progression "Adding system configurations related to $app..."

ynh_config_add_nginx

ynh_config_add_systemd

yunohost service add $app --description="Web File Browser" --log="/var/log/$app/$app.log"

#=================================================
# ADD A CONFIGURATION
#=================================================
ynh_script_progression "Adding $app's configuration..."

ynh_config_add --template="settings.json" --destination="$install_dir/settings.json"

#=================================================
# START SYSTEMD SERVICE
#=================================================
ynh_script_progression "Starting $app's systemd service..."

ynh_systemctl --service=$app --action="start" --log_path="systemd"

#=================================================
# SENDING CREDENTIALS TO ROOT
#=================================================
ynh_script_progression "Sending credentials to admin..."

sleep 5
password=$(journalctl -u $app -n 2 | grep -m1 "admin" | awk '{print $NF}')
ynh_app_setting_set --key=password --value=$password
echo -e "FileBrowser Credentials:\nUsername: admin\nPassword: $password" | mail -s "FileBrowser Installation Details" root

#=================================================
# END OF SCRIPT
#=================================================

ynh_script_progression "Installation of $app completed"
